name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: install glut
      run: apt update && apt install freeglut3-dev
    - name: Install Qt
      uses: jurplel/install-qt-action@v3
    - name: makebuilddir
      run: mkdir gui/build
    - name: makeplaterbuild
      run: mkdir build
    - name: cmake
      working-directory: build
      run: cmake ..
    - name: makeplater
      working-directory: build
      run: make
    - name: qmake
      working-directory: gui/build
      run: qmake ..
    - name: make
      working-directory: gui/build
      run: make
    - name: install
      working-directory: gui/build
      run: sudo make install
